schema: "../api/src/graphql/schema.graphql"
documents:
  - "src/graphql/**/*.graphql"
generates:
  src/graphql/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
    config:
      scalars:
        DateTime: string
        JSON: unknown
